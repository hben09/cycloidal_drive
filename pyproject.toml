[project]
name = "cycloidal-drive"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "cadquery",
    "numpy",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "ocp-vscode",
]