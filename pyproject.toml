[project]
name = "cycloidal-drive"
requires-python = ">=3.12"
dependencies = [
    "cadquery>=2.7.0",
    "numpy>=2.4.2",
    "ocp-vscode>=3.1.2",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
